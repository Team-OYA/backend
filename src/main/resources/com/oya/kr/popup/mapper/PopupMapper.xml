<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oya.kr.popup.mapper.PopupMapper">

    <select id="findById" parameterType="Long" resultType="popupMapperResponse">
        SELECT ID, TITLE, DESCRIPTION, PULLED_DATE, WITHDRAWAL_STATUS, PLAN_ID, CREATED_DATE, MODIFIED_DATE, DELETED
        FROM POPUP
        WHERE ID = #{popupId}
          AND DELETED = 0
    </select>

    <select id="findByIdWithDate" parameterType="popupDetailMapperRequest" resultType="popupDetailMapperResponse">
        SELECT P.ID, TITLE, DESCRIPTION, PULLED_DATE, WITHDRAWAL_STATUS, PLAN_ID, null, PN.USER_ID,
            PN.CATEGORY, OPEN_DATE, CLOSE_DATE,
            NVL2(PC.ID, 1, 0) COLLECTED
        FROM POPUP P
            JOIN PLAN PN
                ON P.PLAN_ID = PN.ID
            LEFT JOIN POPUP_COLLECTION PC
                ON P.ID = PC.POPUP_ID
                AND PC.USER_ID = #{userId}
                AND PC.DELETED = 0
        WHERE P.ID = #{popupId}
            AND P.DELETED = 0
    </select>

    <select id="findAllByPlanId" parameterType="Long" resultType="popupMapperResponse">
        SELECT ID, TITLE, DESCRIPTION, PULLED_DATE, WITHDRAWAL_STATUS, PLAN_ID, CREATED_DATE, MODIFIED_DATE, DELETED
        FROM POPUP
        WHERE PLAN_ID = #{planId} AND DELETED = 0
    </select>

    <select id="findAll" parameterType="popupSearchRequest" resultType="popupDetailMapperResponse">
        <include refid="selectPopupWithPlanAndPopupImage" />
        WHERE WITHDRAWAL_STATUS != #{withdrawalStatus} AND P.DELETED = 0
        ORDER BY PULLED_DATE DESC
        <include refid="offsetForPaging" />
    </select>

    <select id="findInProgress" parameterType="popupSearchRequest" resultType="popupDetailMapperResponse">
        <include refid="selectPopupWithPlanAndPopupImage" />
        WHERE WITHDRAWAL_STATUS != #{withdrawalStatus}
            AND sysdate BETWEEN OPEN_DATE AND CLOSE_DATE
            AND P.DELETED = 0
        ORDER BY PULLED_DATE DESC
        <include refid="offsetForPaging" />
    </select>

    <select id="findScheduled" parameterType="popupSearchRequest" resultType="popupDetailMapperResponse">
        <include refid="selectPopupWithPlanAndPopupImage" />
        <![CDATA[
        WHERE WITHDRAWAL_STATUS != #{withdrawalStatus}
            AND sysdate < OPEN_DATE
            AND P.DELETED = 0
        ORDER BY PULLED_DATE DESC
        ]]>
        <include refid="offsetForPaging" />
    </select>

    <select id="findCollections" parameterType="popupSearchRequest" resultType="popupDetailMapperResponse">
        <include refid="selectPopupWithPlanAndPopupImage" />
            JOIN POPUP_COLLECTION PC
                ON P.ID = PC.POPUP_ID
                    AND PC.DELETED = 0
        <![CDATA[
        WHERE WITHDRAWAL_STATUS != #{withdrawalStatus}
            AND P.DELETED = 0
        ORDER BY PULLED_DATE DESC
        ]]>
        <include refid="offsetForPaging" />
    </select>

    <select id="findAllRecommended" parameterType="popupSearchRequest" resultType="popupDetailMapperResponse">
        <include refid="selectPopupWithPlanAndPopupImage" />
        LEFT JOIN (
            SELECT POPUP_ID, COUNT(*) COUNT
            FROM POPUP_VIEW
            GROUP BY POPUP_ID
            <include refid="offsetForPaging" />) PV
        ON P.ID = PV.POPUP_ID
        WHERE WITHDRAWAL_STATUS != #{withdrawalStatus}
          AND P.DELETED = 0
        ORDER BY COUNT DESC
    </select>

    <select id="statistics" parameterType="Long" resultType="statisticsPopupMapperResponse">
        SELECT
            (SELECT COUNT(*) FROM POPUP P, PLAN PL WHERE P.PLAN_ID = PL.ID AND PL.USER_ID = #{userId}) AS TOTAL,
            (SELECT COUNT(*) FROM POPUP_ADVERTISEMENT PA, POPUP PU, PLAN PL
                WHERE PA.POPUP_ID = PU.ID AND PU.PLAN_ID = PL.ID AND PL.USER_ID = #{userId}) AS AD
        FROM dual
    </select>

    <insert id="save" parameterType="popupSaveMapperRequest">
        INSERT INTO POPUP(TITLE, DESCRIPTION, WITHDRAWAL_STATUS, PLAN_ID)
        VALUES (#{title}, #{description}, #{withdrawalStatus}, #{planId})
        <selectKey keyProperty="popupId" resultType="long" order="AFTER">
            SELECT NVL(MAX(ID), 0)
            FROM POPUP
        </selectKey>
    </insert>

    <delete id="deleteAll">
        DELETE
        FROM POPUP
    </delete>

    <sql id="selectPopupWithPlanAndPopupImage">
        SELECT P.ID, TITLE, DESCRIPTION, PULLED_DATE, WITHDRAWAL_STATUS, PLAN_ID,
            PI.URL thumbnail, PN.USER_ID userId, PN.CATEGORY,
            OPEN_DATE, CLOSE_DATE, 1
        FROM POPUP P
            LEFT JOIN POPUP_IMAGE PI
                ON P.ID = PI.POPUP_ID
                    AND PI.DELETED = 0
            JOIN PLAN PN
                ON P.PLAN_ID = PN.ID
                    AND PN.DELETED = 0
    </sql>

    <sql id="offsetForPaging">
        OFFSET #{pageNo} ROWS FETCH NEXT #{amount} ROWS ONLY
    </sql>

    <!--
   내가 작성한 팝업 조회 여부 반환

    @return boolean
    @author 이상민
    @since 2024.02.28
   -->
    <select id="findMe" >
        <include refid="selectPopupWithPlanAndPopupImage" />
        WHERE WITHDRAWAL_STATUS != #{request.withdrawalStatus} AND P.DELETED = 0 AND PN.USER_ID = #{userId}
        ORDER BY PULLED_DATE DESC
        OFFSET #{request.pageNo} ROWS FETCH NEXT #{request.amount} ROWS ONLY
    </select>

    <!--
    팝업스토어 순위

    @return PopupTopMapperResponse
    @author 이상민
    @since 2024.02.29
   -->
    <select id="findByTop" resultType="com.oya.kr.popup.mapper.dto.response.PopupTopMapperResponse">
        SELECT pl.USER_ID, p.ID as planId, p.TITLE, COUNT(p.ID) AS viewSum
        FROM POPUP p
                 LEFT JOIN POPUP_VIEW pv ON p.ID = pv.POPUP_ID
                 LEFT JOIN PLAN pl ON p.PLAN_ID = pl.ID
                 LEFT JOIN USERS u ON u.ID = pl.USER_ID
        GROUP BY p.ID, p.TITLE, pl.USER_ID
        ORDER BY viewSum desc
    </select>

    <!--
    사업계획서에 따른 팝업스토어 상세 정보 보기

    @return MyPopupDetailMapper
    @author 이상민
    @since 2024.03.01
   -->
    <select id="findByPlanId" resultType="com.oya.kr.popup.mapper.dto.response.MyPopupDetailMapper">
        SELECT p.ID,
               p.TITLE,
               p.DESCRIPTION,
               p.CREATED_DATE,
               p.MODIFIED_DATE,
               p.WITHDRAWAL_STATUS,
               pa.AMOUNT,
               COUNT(pv.POPUP_ID) as popupView
        FROM POPUP p
                 LEFT JOIN PLAN pl ON pl.ID = p.PLAN_ID
                 LEFT JOIN POPUP_VIEW pv ON p.ID = pv.POPUP_ID
                 LEFT JOIN POPUP_ADVERTISEMENT pa ON p.ID = pa.POPUP_ID
        WHERE p.PLAN_ID = #{planId}
        GROUP BY p.WITHDRAWAL_STATUS, p.ID, p.TITLE, p.DESCRIPTION, p.CREATED_DATE, p.MODIFIED_DATE, pa.AMOUNT
    </select>

</mapper>
